<view class="contain">
    <view class="list" wx:for="{{list}}" wx:key="{{item.id}}">
        <image class="item" src="{{item.product_cover}}" data-id="{{item.id}}" width="355" height="150" bindtap='handleOpenVipGoodsDetail'/>
	</view>
</view>